# backend

Reserved for the Kairos backend (API, image-ingest pipeline, embedding store).
Empty for now — the frontend in `../frontend` runs against static mock data
defined in `frontend/src/data/data.js`.

When you scaffold the backend here, wire its CI in
`.github/workflows/backend.yml` (mirroring `frontend.yml`).
